mutation CreateUser($options: UsernamePasswordInput!) {
  createUser(options: $options) {
    user {
      username
      email
      password
    }
    errors {
      field
      message
    }
  }
}
